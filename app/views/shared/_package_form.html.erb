<%= form_with(model: @package, local: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :adress, placeholder: "Give me the adress for package..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
